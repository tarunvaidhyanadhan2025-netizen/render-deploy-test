# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# ── Settings ────────────────────────────────────────────────────────────────────
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1

# ── Database ─────────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
alembic==1.13.2

# ── OCR ──────────────────────────────────────────────────────────────────────────
pytesseract==0.3.10
Pillow==10.4.0
# easyocr==1.7.1  # Uncomment if OCR_PROVIDER=easyocr

# ── LLM Providers ────────────────────────────────────────────────────────────────
# Install all so the fallback chain works. Unused providers are imported lazily.
google-genai==2.8.0                # MODEL_PROVIDER=gemini (replaces deprecated google-generativeai)
openai==1.35.7                     # MODEL_PROVIDER=openai or fallback
# transformers==4.42.4             # MODEL_PROVIDER=huggingface (optional)
# torch==2.3.1                     # MODEL_PROVIDER=huggingface (optional)
# bitsandbytes==0.43.1             # HF_LOAD_IN_4BIT=true (optional)
# accelerate==0.31.0               # MODEL_PROVIDER=huggingface (optional)

# ── Utilities ─────────────────────────────────────────────────────────────────────
httpx==0.27.0
aiofiles==23.2.1
tenacity==8.4.1

# ── Testing ──────────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# ── Dev ───────────────────────────────────────────────────────────────────────────
black==24.4.2
ruff==0.4.10
